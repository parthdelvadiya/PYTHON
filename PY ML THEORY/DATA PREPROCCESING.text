Data preprocessing = converting raw data into a clean, structured, and model-ready format

Raw Data
   ↓
Data Cleaning
   ↓
Data Preprocessing
   ↓
Model Training

df.fillna(df.mean())     # fill with mean
df.dropna()              # remove rows

Encoding Categorical Data
    pd.get_dummies(df['City'])

Label Encoding (Ordinal data)
    from sklearn.preprocessing import LabelEncoder

    le = LabelEncoder()
    df['Level'] = le.fit_transform(df['Level'])

Feature Scaling ⭐ (VERY IMPORTANT)
   Makes all features in same range
      
      
      Standardization
         Converts data to mean = 0, std = 1
               Subtract mean → center data around 0
               Divide by std (Standard deviation) → scale spread
      
      from sklearn.preprocessing import StandardScaler

      scaler = StandardScaler()
      X_scaled = scaler.fit_transform(X)

      Normalization
         Converts data to range [0,1]

      from sklearn.preprocessing import MinMaxScaler

      scaler = MinMaxScaler()
      X_scaled = scaler.fit_transform(X)



| Feature  | Standardization | Normalization        |
| -------- | --------------- | -------------------- |
| Range    | Not fixed       | 0 to 1               |
| Outliers | Handles better  | Sensitive            |
| Use      | Most ML models  | Neural networks, KNN |


Handling Outliers
   IQR
   Z-score
   Capping
   df = df[(df['salary'] >= lower) & (df['salary'] <= upper)]

Feature Selection
   Remove unnecessary features
   df.drop(['id'], axis=1)


“Data preprocessing involves transforming raw data into a suitable format 
for machine learning by handling missing values, encoding categorical variables, 
scaling features, removing outliers, selecting important features, and splitting 
the data into training and testing sets.”