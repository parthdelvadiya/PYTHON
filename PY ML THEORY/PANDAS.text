What is Pandas?

Pandas is used for:
    data loading
    data cleaning
    preprocessing
    feature engineering
    missing value handling
    analysis

Series → 1D
DataFrame → 2D table

import pandas as pd

df = pd.DataFrame({
    "Name": ["Parth", "Rahul", "Aman"],
    "Age": [21, 22, 23],
    "Salary": [50000, 60000, 55000]
})

print(df)

    Name  Age  Salary
0  Parth   21   50000
1  Rahul   22   60000
2   Aman   23   55000

print(df.shape)
(3, 3)

print(df.columns)
Index(['Name', 'Age', 'Salary'], dtype='object')

df.info()

<class 'pandas.core.frame.DataFrame'>
RangeIndex: 3 entries, 0 to 2
Data columns (total 3 columns):
 Name      3 non-null object
 Age       3 non-null int64
 Salary    3 non-null int64


print(df.describe())

        Age    Salary
count   3.000000      3.0
mean   22.000000  55000.0
std     1.000000   5000.0
min    21.000000  50000.0
max    23.000000  60000.0

print(df["Age"])

print(df.loc[1])

Name      Rahul
Age          22
Salary    60000
Name: 1, dtype: object

print(df.iloc[0:2])

    Name  Age  Salary
0  Parth   21   50000
1  Rahul   22   60000

print(df[df["Age"] > 21])
    Name  Age  Salary
1  Rahul   22   60000
2   Aman   23   55000

print(df.isnull())
    Name    Age  Salary
0  False  False   False
1  False  False   False
2  False  False   False


print(df.isnull().sum())

df.dropna()

df["Age"].fillna(df["Age"].mean())


🎯 THE GOLDEN RULE

This is the best way to remember:
axis = dimension that gets reduced / removed

df.drop("Age", axis=1)

Here axis=1 means:
operate on columns
So it removes the column


Difference Between loc[] and iloc[]

Both are used to select rows and columns from a DataFrame.

The main difference is:

✅ loc[] → label / name based
✅ iloc[] → integer position / index based


import pandas as pd

df = pd.DataFrame({
    "Name": ["Parth", "Rahul", "Aman"],
    "Age": [21, 22, 23],
    "Salary": [50000, 60000, 55000]
})

print(df)

    Name  Age  Salary
0  Parth   21   50000
1  Rahul   22   60000
2   Aman   23   55000

df.loc[row_label, column_name]

print(df.loc[1, "Name"])
Rahul

print(df.loc[:, ["Name", "Salary"]])
    Name  Salary
0  Parth   50000
1  Rahul   60000
2   Aman   55000

🔹 iloc[] → Integer Position Based

df.iloc[row_position, column_position]
print(df.iloc[1])
Name      Rahul
Age          22
Salary    60000
Name: 1, dtype: object

print(df.iloc[1, 0])
Rahul

loc slice → end INCLUDED
print(df.loc[0:1])

iloc slice → end EXCLUDED
print(df.iloc[0:1])

X = df.iloc[:, 1:3]
y = df.loc[:, "Salary"]